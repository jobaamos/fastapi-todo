services:
  - type: web
    name: fastapi-todo
    env: python
    buildCommand: pip install -r requirements.txt
    uvicorn todo_api:app --host 0.0.0.0 --port 10000
    plan: free
    autoDeploy: true
